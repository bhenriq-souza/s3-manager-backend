{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Debug Hello World",
      "program": "${workspaceFolder}/node_modules/.bin/lambda-local",
      "args": [
        "-l",
        "${workspaceFolder}/dist/hello.js",
        "-h",
        "handler",
        "-e",
        "${workspaceFolder}/tests/mocks/hello.json",
        "-t",
        "10"
      ],
      "console": "integratedTerminal"
    }
  ]
}